function searchLocation(location){
    //need to sign up to get apikey to use this api
    //query with fetch using location
    //if query does not return location, give alert() with notice
    //if query returns location, get needed data
    //get the id of an empty div used to show the cards
    //change the innerHTML to show the information on a card
    //use ${} when need to use variable
}